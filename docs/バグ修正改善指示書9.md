# KeyPass (SecureVault) Autofill 完全修正指示書

## 背景と根本原因

登録済みサイトで自動入力が動作しない問題には **2つの独立した根本原因** がある。

### 原因A: Chromium 137 以降の compatibility-mode 廃止（ブラウザ側の仕様変更）

Chromium 137 以降（Chrome, Brave, Edge 含む）は `compatibility-package` による
互換モードを廃止した。現在の `autofill_service_config.xml` では
`maxLongVersionCode="999999999"` を指定しており、**これが Chromium 137+ で
自動入力フレームワークの正常な動作を妨げている**。

Google 公式ドキュメント（2025年更新）の推奨:
```xml
<compatibility-package android:name="com.android.chrome" android:maxLongVersionCode="711900039" />
加えて、Chromium 137 以降のブラウザでは、ユーザーがブラウザ設定で 「Autofill using another service（別のサービスを使用して自動入力）」 を 明示的に有効にする必要がある。アプリ側からユーザーにこの設定を案内する仕組みが必要。

原因B: ネイティブアプリ（Netflix 等）でのマッチング失敗
ログで com.netflix.mediaclient に対し webDomain=null かつ fromPackage=0 で no match found となっている。 CSV インポート時に packageName が保存されないため、ネイティブアプリでは パッケージ名ベースのマッチングが不可能。serviceName ベースの フォールバック検索も十分に機能していない。

修正対象ファイル
#	ファイルパス	変更内容
1	app/src/main/res/xml/autofill_service_config.xml	compatibility-package の maxLongVersionCode を修正
2	app/src/main/java/com/securevault/app/service/autofill/SecureVaultAutofillService.kt	resolveCredentials のフォールバック改善、ブラウザ設定ガイド用通知
3	app/src/main/java/com/securevault/app/data/db/dao/CredentialDao.kt	serviceName による直接検索クエリ追加
4	app/src/main/java/com/securevault/app/data/repository/CredentialRepository.kt	インターフェースにメソッド追加
5	app/src/main/java/com/securevault/app/data/repository/CredentialRepositoryImpl.kt	実装追加
修正1: autofill_service_config.xml の compatibility-package を修正（致命的）
ファイル: app/src/main/res/xml/autofill_service_config.xml
現在のファイルを以下の内容で 全体を置き換える。

Copy<?xml version="1.0" encoding="utf-8"?>
<autofill-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:settingsActivity="com.securevault.app.ui.MainActivity">

    <!--
      Chromium 137+ は compatibility-mode を廃止し、ネイティブ Android Autofill を使用する。
      maxLongVersionCode を 136 系の最終バージョンに制限する（Google 公式推奨値）。
      137 以降のユーザーはブラウザ設定で「別のサービスを使用して自動入力」を有効にする必要がある。
    -->
    <compatibility-package android:name="com.android.chrome" android:maxLongVersionCode="711900039" />
    <compatibility-package android:name="com.chrome.beta" android:maxLongVersionCode="711900039" />
    <compatibility-package android:name="com.chrome.dev" android:maxLongVersionCode="711900039" />
    <compatibility-package android:name="com.chrome.canary" android:maxLongVersionCode="711900039" />
    <compatibility-package android:name="com.brave.browser" android:maxLongVersionCode="711900039" />
    <compatibility-package android:name="com.microsoft.emmx" android:maxLongVersionCode="711900039" />
    <compatibility-package android:name="org.mozilla.firefox" android:maxLongVersionCode="999999999" />
    <compatibility-package android:name="org.mozilla.firefox_beta" android:maxLongVersionCode="999999999" />
    <compatibility-package android:name="com.sec.android.app.sbrowser" android:maxLongVersionCode="999999999" />
    <compatibility-package android:name="com.opera.browser" android:maxLongVersionCode="999999999" />
    <compatibility-package android:name="com.opera.mini.native" android:maxLongVersionCode="999999999" />
    <compatibility-package android:name="com.vivaldi.browser" android:maxLongVersionCode="999999999" />
    <compatibility-package android:name="com.duckduckgo.mobile.android" android:maxLongVersionCode="999999999" />
</autofill-service>
ポイント:

Chromium ベースブラウザ（Chrome, Chrome Beta, Chrome Dev, Chrome Canary, Brave, Edge）は 711900039 に制限
Firefox, Samsung Internet, Opera, Vivaldi, DuckDuckGo は Chromium ベースでないか、この変更の対象外なので 999999999 のまま
com.chrome.canary を新規追加（公式推奨）
修正2: CredentialDao にドメイン部分一致検索を追加
ファイル: app/src/main/java/com/securevault/app/data/db/dao/CredentialDao.kt
以下のメソッドを CredentialDao インターフェースに 追加 する（既存メソッドは変更しない）。

Copy/** serviceName をドメイン部分一致で検索する（Autofill 用）。 */
@Query("SELECT * FROM credentials WHERE serviceName LIKE '%' || :domain || '%' OR serviceUrl LIKE '%' || :domain || '%' ORDER BY serviceName ASC")
suspend fun findByDomain(domain: String): List<CredentialEntity>
修正3: CredentialRepository インターフェースにメソッド追加
ファイル: app/src/main/java/com/securevault/app/data/repository/CredentialRepository.kt
以下のメソッドを 追加 する（既存メソッドは変更しない）。

Copy/** ドメインで serviceName / serviceUrl を横断検索する（Autofill 用）。 */
suspend fun findByDomain(domain: String): List<Credential>
修正4: CredentialRepositoryImpl に実装追加
ファイル: app/src/main/java/com/securevault/app/data/repository/CredentialRepositoryImpl.kt
以下のメソッドを 追加 する（既存メソッドは変更しない）。

Copy/** ドメインで serviceName / serviceUrl を横断検索する。 */
override suspend fun findByDomain(domain: String): List<Credential> {
    return credentialDao.findByDomain(domain).mapNotNull { it.toDomainOrNull() }
}
修正5: SecureVaultAutofillService の resolveCredentials を改善
ファイル: app/src/main/java/com/securevault/app/service/autofill/SecureVaultAutofillService.kt
resolveCredentials メソッドを以下の内容で 全体を置き換える。

Copyprivate suspend fun resolveCredentials(packageName: String, webDomain: String?): List<Credential> {
    logger.d(TAG, "resolveCredentials: package=$packageName, webDomain=$webDomain")

    // 1. パッケージ名で完全一致検索
    if (packageName.isNotBlank()) {
        val fromPackage = credentialRepository.findByPackageName(packageName)
        logger.d(TAG, "resolveCredentials: fromPackage=${fromPackage.size}")
        if (fromPackage.isNotEmpty()) {
            return fromPackage.take(MAX_DATASET_ITEMS)
        }
    }

    // 2. webDomain があれば URL 部分一致で検索
    if (!webDomain.isNullOrBlank()) {
        val fromUrl = credentialRepository.findByUrl(webDomain)
        logger.d(TAG, "resolveCredentials: fromUrl=${fromUrl.size} domain=$webDomain")
        if (fromUrl.isNotEmpty()) {
            return fromUrl.take(MAX_DATASET_ITEMS)
        }

        // 3. serviceName / serviceUrl 横断でドメイン部分一致検索
        val fromDomain = credentialRepository.findByDomain(webDomain)
        logger.d(TAG, "resolveCredentials: fromDomain=${fromDomain.size} domain=$webDomain")
        if (fromDomain.isNotEmpty()) {
            return fromDomain.take(MAX_DATASET_ITEMS)
        }

        // 4. メインドメイン（例: id.kuronekoyamato.co.jp → kuronekoyamato）で検索
        val mainDomain = extractMainDomain(webDomain)
        if (!mainDomain.isNullOrBlank() && mainDomain != webDomain.lowercase()) {
            val fromMainDomain = credentialRepository.findByDomain(mainDomain)
            logger.d(TAG, "resolveCredentials: fromMainDomain=${fromMainDomain.size} mainDomain=$mainDomain")
            if (fromMainDomain.isNotEmpty()) {
                return fromMainDomain.take(MAX_DATASET_ITEMS)
            }
        }
    }

    // 5. webDomain がない場合（ネイティブアプリ）、パッケージ名からサービス名を推測して検索
    if (webDomain.isNullOrBlank() && packageName.isNotBlank()) {
        val appName = packageName.substringAfterLast('.')
        if (appName.isNotBlank() && appName.length >= 3) {
            val fromAppName = credentialRepository.findByDomain(appName)
            logger.d(TAG, "resolveCredentials: fromAppName=${fromAppName.size} appName=$appName")
            if (fromAppName.isNotEmpty()) {
                return fromAppName.take(MAX_DATASET_ITEMS)
            }
        }

        // パッケージ名の主要部分（例: com.netflix.mediaclient → netflix）で検索
        val parts = packageName.split(".")
        val significantPart = parts.firstOrNull { it.length >= 3 && it != "com" && it != "app" && it != "android" && it != "mobile" && it != "client" }
        if (significantPart != null && significantPart != appName) {
            val fromSignificant = credentialRepository.findByDomain(significantPart)
            logger.d(TAG, "resolveCredentials: fromSignificant=${fromSignificant.size} part=$significantPart")
            if (fromSignificant.isNotEmpty()) {
                return fromSignificant.take(MAX_DATASET_ITEMS)
            }
        }
    }

    logger.d(TAG, "resolveCredentials: no match found")
    return emptyList()
}
Copy
修正6: ブラウザ設定ガイドの初回通知（推奨）
Chromium 137+ のブラウザでは、ユーザーがブラウザ設定で「別のサービスを使用して自動入力」を 有効にしなければ自動入力が動作しない。onFillRequest でブラウザからのリクエストなのに フィールドが検出されない場合や、初回接続時にユーザーへ設定変更を促す通知を出すことを推奨する。

SecureVaultAutofillService.kt に以下のメソッドを追加
Copy/**
 * Chromium ベースブラウザからの初回リクエスト時に、ブラウザ側の設定案内通知を表示する。
 */
private fun showBrowserSettingsGuideIfNeeded(targetPackageName: String) {
    if (targetPackageName !in CHROMIUM_BROWSER_PACKAGES) return

    val prefs = getSharedPreferences(BROWSER_GUIDE_PREFS, MODE_PRIVATE)
    val key = "guide_shown_$targetPackageName"
    if (prefs.getBoolean(key, false)) return

    prefs.edit().putBoolean(key, true).apply()

    if (
        Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU &&
        checkSelfPermission(Manifest.permission.POST_NOTIFICATIONS) != PackageManager.PERMISSION_GRANTED
    ) {
        return
    }

    val notification = NotificationCompat.Builder(this, AUTOFILL_SAVE_CHANNEL_ID)
        .setSmallIcon(R.mipmap.ic_launcher)
        .setContentTitle("ブラウザの自動入力設定")
        .setContentText("ブラウザ設定 → 自動入力サービス → 「別のサービスを使用」を有効にしてください")
        .setStyle(NotificationCompat.BigTextStyle().bigText(
            "Chrome/Brave/Edge のバージョン137以降では、ブラウザ設定で「別のサービスを使用して自動入力」を有効にする必要があります。\n\nブラウザ → 設定 → 自動入力サービス → 「別のサービスを使用して自動入力」を選択してください。"
        ))
        .setPriority(NotificationCompat.PRIORITY_HIGH)
        .setAutoCancel(true)
        .build()

    runCatching {
        NotificationManagerCompat.from(this).notify(
            BROWSER_GUIDE_NOTIFICATION_ID + abs(targetPackageName.hashCode()),
            notification
        )
    }
}
Copy
companion object に以下の定数を追加
Copyconst val BROWSER_GUIDE_PREFS = "browser_guide_prefs"
const val BROWSER_GUIDE_NOTIFICATION_ID = 13000

val CHROMIUM_BROWSER_PACKAGES = setOf(
    "com.android.chrome",
    "com.chrome.beta",
    "com.chrome.dev",
    "com.chrome.canary",
    "com.brave.browser",
    "com.microsoft.emmx",
    "com.vivaldi.browser"
)
onFillRequest 内の targets 検出直後に呼び出しを追加
Copyval targets = smartFieldDetector.detect(structure)
// ★ この行を追加
showBrowserSettingsGuideIfNeeded(targetPackageName)
修正後の確認手順
ステップ1: ブラウザ側の設定を変更する（ユーザー操作が必要）
Brave を開く
設定 → パスワードと自動入力（または自動入力サービス）
「別のサービスを使用して自動入力」 を選択
Brave を再起動
Chrome/Edge でも同様の操作が必要。

ステップ2: アプリ側の動作確認
KeyPass でパスワードがインポート済みであることを確認（30件）
Brave で id.kuronekoyamato.co.jp にアクセスし、ログインフォームをタップ
KeyPass の候補が表示され、タップすると username/password が入力されること
Netflix アプリを開き、ログイン画面で KeyPass の候補が表示されること
Bitwarden 等のパスワードマネージャーでは候補が表示されないこと
制約事項
既存のメソッドシグネチャは変更しない（追加のみ）
新規ファイルの追加は不要
import 文は必要に応じて追加すること
コメントは日本語で記述する
Context.MODE_PRIVATE を使用する箇所で import が必要な場合は追加する
要約: なぜ今まで動かなかったか
コードは実は概ね正しく動いていた。 ログで === onFillRequest END (success) === と出ていたのがその証拠。FillResponse は正しく構築され、Android OS に返されていた。

しかし Brave/Chrome/Edge（Chromium 137+）は 2025年6月以降、compatibility-mode を廃止 しており、autofill_service_config.xml で maxLongVersionCode="999999999" を指定していると、ブラウザが互換モードを無視する ため、ブラウザ内の WebView フォームに対する自動入力が機能しなかった。

加えて、ブラウザ側で 「別のサービスを使用して自動入力」 がデフォルトで OFF のため、ユーザーが明示的にこの設定を有効にしなければ、Android Autofill フレームワーク自体がブラウザに対して無効化されていた。

ネイティブアプリ（Netflix 等）については、compatibility-mode は関係ないが、CSV インポートで packageName が保存されないため、パッケージ名ベースのマッチングが効かず、候補が表示されなかった。